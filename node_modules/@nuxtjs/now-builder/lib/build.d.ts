import { FileFsRef, BuildOptions, Route, Lambda, File } from '@now/build-utils';
interface BuilderOutput {
    watch?: string[];
    output: Record<string, Lambda | File | FileFsRef>;
    routes: Route[];
}
export declare function build({ files, entrypoint, workPath, config, meta }: BuildOptions): Promise<BuilderOutput>;
export {};
